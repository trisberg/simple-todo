# Environment Configuration for Simple Todo App
# Copy this file to config.properties and modify the values as needed

# Database Configuration
DATABASE_URL=jdbc:postgresql://localhost:5432/simpletodo
DATABASE_USERNAME=todouser
DATABASE_PASSWORD=todopass

# Application Configuration
SPRING_PROFILES_ACTIVE=prod
SERVER_PORT=8080

# PostgreSQL Configuration (for Docker)
POSTGRES_DB=simpletodo
POSTGRES_USER=todouser
POSTGRES_PASSWORD=todopass

# pgAdmin Configuration
PGADMIN_DEFAULT_EMAIL=admin@simpletodo.com
PGADMIN_DEFAULT_PASSWORD=admin
